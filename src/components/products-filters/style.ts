import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    paddingTop: 12,
    backgroundColor: '#ffffff',
    marginBottom: 36,
  },
  scrollContent: {
    marginHorizontal: 16,
  },
  space: {
    marginBottom: 24,
    marginHorizontal: 12,
  },
});
